// Copyright (c) FIRST and other WPILib contributors.
// Open Source Software; you can modify and/or share it under the terms of
// the WPILib BSD license file in the root directory of this project.

package frc.robot.commands;
import static frc.robot.Constants.autonomousCommand;
import edu.wpi.first.wpilibj2.command.Command;

// import frc.robot.subsystems.CANLauncher;

/*This is an example of creating a command as a class. The base Command class provides a set of methods that your command
 * will override.
 */
//public final class m_autonomousCommand {
  

  //STEP 1: Change this to drive stuff.
  /*
    RobotLauncher launcher;

  // CANLauncher m_launcher;

  // Creates a new LaunchNote.
  public LaunchNote(RobotLauncher newlauncher) {
    // save the launcher system internally
    launcher = newlauncher;

    // indicate that this command requires the launcher system
    addRequirements(launcher);
  }
  */

  // The initialize method is called when the command is initially scheduled.
  //@Override
  //public void initialize() {
    //STEP 2: bring in any 1-time stuff here.  maybe encoders, maybe setup drive subsystem constants or distances.  Probably need a constant here ready from constants.java for the exact encoder value of 10' brought in, get it ready for step 3.
    

  //}

  // Called every time the scheduler runs while the command is scheduled.
  /*@Override
  public void execute() {
    // There is nothing we need this command to do on each iteration. You could remove this method
    // and the default blank method
    // of the base class will run.
  
    //STEP 3: write an if/elseif/else conditional here for bringing in encoder data, checking it for 10' or more, otherwise or "else if" greater than or equal to 10' driven based on the encoder
  
    //STEP 4: call isFinished() when the condition of more than 10' has been reached.
  
  }

  // Returns true when the command should end.
  @Override
  public boolean isFinished() {
    // Always return false so the command never ends on it's own. In this project we use the
    // scheduler to end the command when the button is released.
    return false;
  }

  // Called once the command ends or is interrupted.
  @Override
  public void end(boolean interrupted) {
    // Stop the wheels when the command ends.
    launcher.stop();
  }
}
*/
